[project]
name = "kubenexus"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Hridesh", email = "hridesh.mehrotra@gmail.com" }
]
requires-python = ">=3.10"
dependencies = [
    "sentence-transformers>=6.0.0",
]

[project.scripts]
kubenexus = "kubenexus:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
